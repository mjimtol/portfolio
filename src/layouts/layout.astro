---
import { ViewTransitions } from "astro:transitions";

interface Props {
  // title: string;
}

// const { title } = Astro.props;
---

<ViewTransitions />
<section class="bg-neutral-900 h-screen w-screen text-white">
  <slot />
</section>
