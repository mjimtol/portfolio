<section class="intro">
  <div class="intro-content">
    <div>
      <h2>
        <span>M</span><span>A</span><span>G</span><span>E</span><span>C</span>
        <span>J</span><span>I</span><span>M</span><span>É</span><span>N</span
        ><span>E</span><span>Z</span>
      </h2>
      <!-- <h1>Desarrollador</h1> -->
    </div>
    <div>
      <img src="/foto.png" alt="Foto de perfil" width="300" />
    </div>
  </div>
</section>

<style>
  :root {
    --delay-base: 0.12s;
  }
  .intro {
    width: 85vw;
    height: 150vh;
    margin: auto;
    /* border: 1px solid white; */
  }

  .intro-content {
    display: flex;
    justify-content: space-around;
    position: sticky;
    top: 0;
    height: 100vh;
    align-items: center;
  }

  .intro-content h2 {
    font-family: monospace;
    font-size: 6rem;
    text-transform: uppercase;
    font-weight: 600;
    color: var(--accent);
    opacity: 1;
  }

  .intro-content h2 span {
    display: inline-block;
    transform: translateX(0);

    animation: goLeft 0.6s ease-in-out both;
    animation-timeline: view(0% 60%);
  }
  .intro-content h2 span:nth-child(1) {
    animation-delay: calc(var(--delay-base) * 0);
  }
  .intro-content h2 span:nth-child(2) {
    animation-delay: calc(var(--delay-base) * 1);
  }
  .intro-content h2 span:nth-child(3) {
    animation-delay: calc(var(--delay-base) * 2);
  }
  .intro-content h2 span:nth-child(4) {
    animation-delay: calc(var(--delay-base) * 3);
  }
  .intro-content h2 span:nth-child(5) {
    animation-delay: calc(var(--delay-base) * 4);
  }
  .intro-content h2 span:nth-child(6) {
    animation-delay: calc(var(--delay-base) * 5);
  }
  .intro-content h2 span:nth-child(7) {
    animation-delay: calc(var(--delay-base) * 6);
  }
  .intro-content h2 span:nth-child(8) {
    animation-delay: calc(var(--delay-base) * 7);
  }
  .intro-content h2 span:nth-child(9) {
    animation-delay: calc(var(--delay-base) * 8);
  }
  .intro-content h2 span:nth-child(10) {
    animation-delay: calc(var(--delay-base) * 9);
  }
  .intro-content h2 span:nth-child(11) {
    animation-delay: calc(var(--delay-base) * 10);
  }
  .intro-content h2 span:nth-child(12) {
    animation-delay: calc(var(--delay-base) * 11);
  }

  @keyframes goLeft {
    to {
      transform: translateX(-1500px);
    }
  }
</style>
